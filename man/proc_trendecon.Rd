% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/proc_trendecon.R
\name{proc_trendecon}
\alias{proc_trendecon}
\title{Process all indices for one country, tolerant of partial failure}
\usage{
proc_trendecon(geo, indices, backfill_from = NULL)
}
\arguments{
\item{geo}{Two-letter geography, e.g. \code{"CH"}.}

\item{indices}{Named list mapping each index name to its keyword vector.
Indices are processed in list order.}

\item{backfill_from}{Optional start date (\code{YYYY-mm-dd}) passed to
\code{\link[=proc_index]{proc_index()}} / \code{\link[=proc_keyword_backfill_daily]{proc_keyword_backfill_daily()}} to fill a daily gap left by
an outage. Use once; leave \code{NULL} for normal daily runs.}
}
\value{
Invisibly, a data frame with one row per index and columns
\code{geo}, \code{index}, \code{ok}, \code{error}. Stops with an error only if \emph{every} index
failed.
}
\description{
Internal worker behind \code{\link[=proc_trendecon_ch]{proc_trendecon_ch()}}, \code{\link[=proc_trendecon_de]{proc_trendecon_de()}} and
\code{\link[=proc_trendecon_at]{proc_trendecon_at()}}. Each index is processed in isolation: if one index
(or a keyword within it) fails, the remaining indices still run, and every
index that succeeds is copied to \verb{data/<geo>} immediately. This prevents a
single bad keyword or a transient Google Trends error from zeroing out the
entire daily run.
}
\keyword{internal}
