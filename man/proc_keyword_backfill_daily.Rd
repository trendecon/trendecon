% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/proc_keyword_backfill_daily.R
\name{proc_keyword_backfill_daily}
\alias{proc_keyword_backfill_daily}
\title{Backfill the daily series of a keyword over a gap}
\usage{
proc_keyword_backfill_daily(
  keyword = "Insolvenz",
  geo = "CH",
  from = NULL,
  wait = 10,
  retry = 20
)
}
\arguments{
\item{keyword}{A single keyword for which to process the data.}

\item{geo}{A character vector denoting the geographic region.
Default is "CH".}

\item{from}{Start date (\code{YYYY-mm-dd}) of the daily backfill. Defaults to one
month before the last existing daily observation, which comfortably covers
the gap.}
}
\value{
Invisibly \code{TRUE}.
}
\description{
The daily Google Trends download in \code{\link[=proc_keyword_latest]{proc_keyword_latest()}} only covers the
last ~90 days. After an interruption of the daily run, the stored daily series
therefore has a hole: everything between the last pre-outage observation and
~90 days ago is missing. \code{proc_combine_freq()} uses the daily series as the
high-frequency indicator for \code{tempdisagg::td()}, which cannot disaggregate
across a hole and fails. This function refills the daily series over the gap
using the same efficient overlapping windows as \code{\link[=proc_keyword_init]{proc_keyword_init()}}, so that
the combine and seasonal-adjustment steps work again and the resulting series
is fully consistent at daily resolution.
}
\details{
Only the daily series needs backfilling: a normal daily run already refreshes
the weekly (one year) and monthly (since 2006) series over their full span.
}
\seealso{
\code{\link[=proc_keyword_latest]{proc_keyword_latest()}}, \code{\link[=proc_keyword_init]{proc_keyword_init()}}
}
